# 任务书：前端开发者（阶段 2-C）

角色：前端开发者。完整需求见 `TASK-spec.md`，GUI 规格重点看 TASK-spec.md 第 4 节，设计依据见 `ARCHITECTURE.md`、`contracts/web-api.md`、`PLAN.md`、`dsh-research.md`（先全部读完再动手）。后端引擎与 API 由其他专家并行实现，你只做 gui/**，对后端只依赖 contracts/web-api.md 的契约。

## 你的范围（只写这些文件，其他一律不碰）
- `gui/DESIGN.md`（第一步必须先写）
- `gui/**` 面板实现（按 PLAN.md 文件树）
- `gui/preview.html`（独立静态预览页）
- `review/` 目录下的截图素材（可选，供 UI 验收用）

禁止触碰：core/**、server/**、tools/**、插件入口、contracts/**、ARCHITECTURE.md、PLAN.md。

## 第一步：设计契约 gui/DESIGN.md
写清（中文）：
- 设计 token：主色/辅助色/中性色（含暗色与浅色两套）、字体族与字号阶梯、间距系统（4px 基准）、圆角/阴影/边框
- 侧边栏入口规格：图标 🧠 + 标题"记忆管理"的呈现
- 4 个面板的组件规格：记忆浏览器（时间线、筛选器、记忆卡片、展开交互）、检索（搜索框、结果行、分数徽章）、配置（表单控件与分组）、导入导出（上传/下载控件）
- 每个面板的空态/加载态/错误态样式
- 相似度分数的视觉编码（如 ≥阈值=绿、0.6~阈值=黄、<0.6=灰，阈值默认 0.75）

## 第二步：面板实现（gui/**）
技术：按 DSH client-plugin 约定（dsh-research.md/ARCHITECTURE.md）；若约定不明，用无框架原生 JS + CSS（ESM），保证可直接被 DSH 壳加载，且对 Web API 的调用封装在独立模块 api.js 中（base URL 可配置，请求失败时回退 mock 数据并在页面提示"演示数据"）。

四个面板（中文 UI）：
1. **记忆浏览器**：时间线视图（按创建时间倒序，时间轴分组或日期分隔线）；筛选器：全部/会话/全局/按标签（标签从数据动态聚合）；记忆卡片显示：摘要（截断 2 行）、相似度评分、创建时间（相对+绝对）、所属会话（session 简称，全局记忆显示"全局"徽标）；点击卡片展开/折叠完整内容（带过渡动画）；支持删除单条（二次确认）与批量清理入口。
2. **记忆检索**：顶部大搜索框（Enter 或 300ms 防抖实时检索）；结果列表：内容摘要 + 相似度分数徽章（按 DESIGN.md 视觉编码）+ 会话/时间；无结果空态；检索耗时展示（latency_ms）。
3. **配置页**：分组表单——短期记忆（最大消息数、最大 token 数）、自动总结（触发轮数阈值）、检索（Top-K、相似度阈值）、存储（上限 MB、清理策略选择）、嵌入模型（local/openai 下拉 + API Key 输入，仅 openai 时显示）；保存按钮（调 API 写 config.json），显示当前值与默认值差异提示；保存成功/失败 toast。
4. **导入导出**：上传区（拖拽或点击选择 JSON/JSONL，显示文件大小，导入后显示导入统计：新增/跳过/失败条数）；导出按钮（下载 .json 备份，含时间戳文件名）；格式说明文案。

## 第三步：独立预览页 gui/preview.html
- 单文件、内联 CSS/JS、零外部依赖（无 CDN、无字体外链），双击即可在浏览器打开
- 内置 mock 数据：≥12 条记忆（覆盖 3 个会话 + 2 条全局 + 3 个不同标签，含 1 条高相似 0.96、1 条中 0.78、1 条低 0.55，用于展示分数视觉编码）
- 渲染全部 4 个面板（用左侧竖排 tab 或顶部 tab 切换，侧边栏入口"🧠 记忆管理"要可见）
- 模拟交互要真实可点：筛选、展开/折叠、搜索（mock 打分）、配置保存（本地 echo）、导入导出（mock 成功 toast）
- 该页是 UI 验收设计师的验收对象，视觉完成度按成品标准，不允许占位符/ lorem

## 视觉质量要求
现代、克制、层次清晰：8px 网格对齐、字号对比（标题/正文/辅助 3 级）、卡片与分区留白充足、hover/active/focus 三态完整、动效 ≤200ms、中英文排版（数字用等宽或 tabular-nums）、暗色模式可用（跟随系统或提供切换）。

## 汇报
最终回复 <25 行摘要：实现清单、采用技术、preview.html 的路径与打开方式、已知的与 web-api.md 的偏差（如有）。

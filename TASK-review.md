# 任务书：UI 视觉验收设计师（阶段 3）

角色：UI 视觉验收设计师——依据设计契约对照实际界面逐项验收，在交付前拦截通用、雷同、粗糙的界面，输出整改清单。你**只读不改码**：不修改任何代码文件，只产出报告与截图。

## 验收对象与依据
- 验收对象：`gui/preview.html`（双击可用的独立预览页）+ `gui/**` 面板实现代码（作为静态走查依据）
- 依据 1：`TASK-spec.md` 第 4 节（GUI 功能需求：侧边栏入口"🧠 记忆管理"、记忆浏览器、检索面板、配置页、导入导出）
- 依据 2：`gui/DESIGN.md`（前端开发者写的设计契约——对照契约逐项检查实现是否走样）
- 依据 3：`ARCHITECTURE.md` / `contracts/web-api.md` 中关于 GUI 的约束

## 验收方法
1. 用截图工具渲染 gui/preview.html（宽 1200、高 900，fullPage；每个面板/Tab 各截一张，展开态与空态尽量覆盖），截图存到 `review/` 目录并命名（如 review/browser-panel.png、review/search-panel.png、review/config-panel.png、review/import-export-panel.png、review/expanded-card.png）。
2. 截图不可用时，退化为对 HTML/CSS 的静态逐段走查（必须逐面板检查 class/结构/token 用法），并在报告中注明"未获得运行时截图，验收为静态走查"。
3. 对照下列清单逐项给结论（通过/不通过/部分通过 + 证据：截图路径或代码行）：

### A. 功能完整性（对照 TASK-spec.md 第 4 节）
- 侧边栏入口：图标 🧠 + 标题"记忆管理"可见
- 记忆浏览器：时间线视图；按会话/全局/标签三类筛选可用；卡片含摘要、相似度评分、创建时间、所属会话；点击展开完整内容
- 检索面板：顶部搜索框；实时/防抖检索；结果含相似度分数
- 配置页：短期窗口（消息数+token 数）、自动总结阈值、Top-K、相似度阈值、存储上限与清理策略、嵌入模型选择——6 组设置齐全
- 导入导出：上传 JSON/JSONL + 下载备份两个入口

### B. 设计契约符合性（对照 gui/DESIGN.md）
- 色彩 token 使用一致（无游离硬编码色值，允许语义化命名）
- 字号阶梯/间距/圆角符合 token；暗色与浅色两套可用
- 分数视觉编码（绿/黄/灰分段）在卡片与检索结果中一致
- 空态/加载态/错误态三态存在且样式符合契约

### C. 视觉质量（通用验收标准，拦截"雷同界面"）
- 层次：页面有清晰的标题区/工具区/内容区分层，主次分明
- 对齐与网格：元素无错位，间距节奏一致（8px 基准）
- 排版：标题/正文/辅助三级字号对比；数字对齐（tabular-nums）；无溢出/截断/换行破碎
- 交互态：hover/active/focus 可见；动效克制（≤200ms）
- 可读性：正文对比度足够（约 ≥4.5:1）；中文排版无异常字距
- 原创度：非"一眼模板"——有至少 3 处体现产品性格的细节（如记忆卡片的信息密度处理、时间线视觉、分数徽章样式），逐一指出

### D. 一致性
- 4 个面板的控件语言统一（按钮/输入框/徽标/标签同一套样式）
- 文案语气统一（中文、专业、不混杂英文术语乱码）

## 产出
`REVIEW.md`（插件根目录，中文），结构：
1. 验收结论（一句话总评 + 通过项数/不通过项数）
2. 逐项验收表（A/B/C/D 全部条目：结论 + 证据路径）
3. 问题清单：按严重度分级——🔴 blocker（阻断交付：功能缺失/契约违反/明显错位）、🟡 major（交付前应修：样式走样/三态缺失/一致性破绽）、🔵 minor（建议项：微调建议）；每条含：位置（面板+元素）、现象（引用截图或代码）、影响、具体修改建议（精确到 CSS 属性/组件改动级别）
4. 截图索引（文件名 + 说明）
5. 放行建议：是否达到"可交付"标准；若否，列出必须先修的 blocker/major 编号

## 汇报
最终回复 <15 行：总评、blocker/major/minor 各几条、放行建议、REVIEW.md 路径。
